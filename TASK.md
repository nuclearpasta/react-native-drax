# LOCAL-1 — Expo SDK 55, Reanimated v4, gesture handler v3 beta

**Source:** local
**Status:** todo
**URL:** 

## Agent Communication

> If you are blocked waiting for user approval or instructions, notify the UI immediately so the user sees "Input needed" in the header:

- Run `openkit activity await-input --message "<what you need>"`

## Workflow Contract (Mandatory)

> You must follow the canonical workflow phases in order and emit each phase checkpoint.

- `openkit activity phase --phase task-started`
- `openkit activity phase --phase pre-hooks-started`
- `openkit activity phase --phase pre-hooks-completed`
- `openkit activity phase --phase implementation-started`
- `openkit activity phase --phase implementation-completed`
- `openkit activity phase --phase post-hooks-started`
- `openkit activity phase --phase post-hooks-completed`

> Before your final summary, run `openkit activity check-flow --json`. If `compliant` is false, you must complete all `missingActions` and rerun until compliant is true.

## Description

Before creating a worktree from `reanimated-v4` branch, be sure to check any local changes that I did locally without committing and/or pushing. That's because I started working there on a new major version.

We need to upgrade to expo 55, reanimated v4, gesture handler v3 beta, while also keeping in mind that react compiler should work for react-native-drax.

That is just a POC. Breaking changes are allowed since we are working on a new major version. You can rewrite anything that you want. Be sure to do everything you can so we will have the best performance and developer experience. 

What I think I did so far:

- added example so we can test our changes. You can update the example app with even more examples so we can test our features correctly.
- added lots of skills for AI agents to use
- etc

I didnt start it with open kit so maybe you should also adapt the project/task whatever to work even better with open kit, by using hooks or whatever. I added skills on the local changes of `reanimated-v4` branch (without pushing/committing)

Here are some competitor libraries that we need to steal from them as much as possible:

- https://github.com/Guccio163/react-native-reshuffled
- https://github.com/MatiPl01/react-native-sortables
- https://github.com/entropyconquers/react-native-reanimated-dnd

You can also create more Drax components to add functionality. Hooks approach would also be nice. We need to be able to tackle any (or at least the most important ones) drag and drop requirement with a nice developer experience.

Let's review what we got so far, inspect the competitor libraries and plan. Maybe break it into future tasks to work with open kit? Let's see how you can help me with this once, it's a hardcore task. I doubt that you can do it, prove me wrong! You are an expert, but you should double check yourself.

---
*Auto-generated by OpenKit. Updated when AI Context notes change.*
